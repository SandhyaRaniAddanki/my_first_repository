#include <stdio.h>

void main() {
     int a,b,c,d;
     
     printf("enter a value:");
     scanf("%d",&a);
     printf("enter a value:")
     scanf("%d",&b);
     c=a%b;
     d=a/b;
     printf("The number of friends in each team is ",%d," and left out is",%d, &a,&b);
     }